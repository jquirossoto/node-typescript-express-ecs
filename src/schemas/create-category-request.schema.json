{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Create categories request",
    "description": "Create categories request schema.",
    "required": [
        "name"
    ],
    "type": "object",
    "properties": {
        "name": {
            "title": "Name",
            "description": "Category name",
            "type": "string",
            "minLength": 3,
            "maxLength": 50
        }
    },
    "additionalProperties": false
}